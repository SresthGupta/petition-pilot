import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Petition Pilot | AI-Powered Petition Verification",
  description:
    "Verify petition signatures 10x faster with AI-powered OCR and intelligent voter matching. Real-time analytics, compliance reports, and a beautiful interface.",
  keywords: [
    "petition verification",
    "signature validation",
    "voter file matching",
    "political petitions",
    "OCR",
    "AI",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
